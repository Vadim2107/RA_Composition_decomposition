import React from 'react';

/**
 * Компонент иконка новости в списке новостей
*/

const NewsIcon = props =>
<img className={props.className} src={props.children} alt='...'/>

export default NewsIcon;
