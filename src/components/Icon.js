import React from 'react';

/**
 * Компонент иконка для новости и услуги в списке новостей и услуг
*/

const Icon = props =>
<img className={props.className} src={props.children} alt='...'/>

export default Icon;
