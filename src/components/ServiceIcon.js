import React from 'react';

/**
 * Компонент иконка услуги в списке услуг
*/

const ServiceIcon = props =>
<img className={props.className} src={props.children} alt='...'/>

export default ServiceIcon;
